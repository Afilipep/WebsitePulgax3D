# MongoDB Configuration (use MONGODB_URI for production services like Render)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/pulgax_3d_store
MONGO_URL=mongodb://localhost:27017
DB_NAME=pulgax_3d_store

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=https://your-frontend-domain.com,http://localhost:3000

# JWT Configuration
JWT_SECRET=sua-chave-jwt-super-segura-de-64-caracteres-ou-mais-para-producao

# Google OAuth
GOOGLE_CLIENT_ID=seu-google-client-id.apps.googleusercontent.com

# Email Configuration (SMTP)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=seu-email@gmail.com
SMTP_PASSWORD=sua-app-password
FROM_EMAIL=noreply@pulgax3d.com
FROM_NAME=Pulgax 3D Store

# Frontend URL (for email links)
FRONTEND_URL=https://your-frontend-domain.com